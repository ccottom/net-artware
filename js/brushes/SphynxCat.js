/* global mouseIsPressed, colorMode, RGB, fill, noStroke, ellipse,
mouseX, mouseY */
window.brushes.SphynxCat = {
  name: 'Sphynx Cat',
  draw: function () {
    if (mouseIsPressed && !window.dragging) {

    }
  }
}
